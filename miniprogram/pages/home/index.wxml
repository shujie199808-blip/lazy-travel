<view class="page">
  <view class="hero card">
    <view class="badge">旅途种草</view>
    <view class="hero-title">不用做攻略，也能玩得很懂行</view>
    <view class="hero-subtitle">
      我们帮你定制欧洲、澳新、土耳其、新加坡、迪拜的专属自由行行程
    </view>
    <view class="hero-actions">
      <button class="primary hero-btn" bindtap="goForm">我想定制一次旅行</button>
      <view class="hero-note">1v1 私域顾问随时跟进，行程说走就走</view>
    </view>
  </view>

  <scroll-view class="mood-tags" scroll-x enable-flex>
    <view class="tag" wx:for="{{moodTags}}" wx:key="tag">{{item}}</view>
  </scroll-view>

  <view class="section-head">
    <view class="section-title">目的地精选</view>
    <view class="section-sub">像刷小红书一样挑灵感，点开即可定制</view>
  </view>

  <view class="waterfall">
    <view
      class="dest-card"
      wx:for="{{destinations}}"
      wx:key="preset"
      data-preset="{{item.preset}}"
      bindtap="goPreset"
    >
      <view
        class="cover"
        style="background: {{item.coverColor}}; height: {{item.coverHeight}}rpx;"
      >
        <view class="cover-overlay">
          <text class="cover-label">{{item.vibe}}</text>
        </view>
      </view>
      <view class="dest-text">
        <view class="dest-title">{{item.title}}</view>
        <view class="dest-subtitle">{{item.subtitle}}</view>
        <view class="dest-meta">
          <view class="chip">{{item.length}}</view>
          <view class="chip highlight">含私域顾问</view>
        </view>
      </view>
    </view>
  </view>

  <view class="cta-fab" bindtap="goForm">
    <view class="cta-text">
      <view class="cta-title">定制一条我的旅行路线</view>
      <view class="cta-sub">聊需求→发方案→不踩雷的小众体验</view>
    </view>
    <view class="cta-btn">马上定制</view>
  </view>
</view>
